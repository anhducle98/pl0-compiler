Symbol table
    stack
    realloc

    methods
        make_table(previous)
        enter(table, name, type, offset)
        enter_proc(table, name, newtable)
        add_width(table, width)
    fields
        table -> pointer to start position in pool
        offset
        parent -> pointer to parent symbol table


    big pool of SymbolTableEntry + pool_size


Syntax directed translation
    define structs
        expression
        statement
        ...
    semantic actions

Type checking
    void
    type_error

Error recovery
    later
