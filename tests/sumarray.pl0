program sumarray;

var a[100], n, i, sum;

begin
    call readi(n);
    for i := 1 to n do
        call readi(a[i]);

    sum := 0;
    for i := 1 to n do
        sum := sum + a[i];

    call writei(sum);
    call writeln;
end.