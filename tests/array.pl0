program array;

const max = 100;
var a[100];

procedure sum(n);
var i, ret;
    procedure getmin(var x; var y);
    begin
        if x > y then x := y;
    end;

    procedure min(n);
    var i, mn;
    begin
        for i := 1 to n do
        begin
            call getmin(mn + a[i], a[i]);
        end;
    end;
begin
    ret := 0;
    for i := 1 to n do
    begin
        ret := ret + a[i];
        call min(i);
    end;
end;

begin
    call sum(max)
end.
