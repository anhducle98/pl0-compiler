program array;

const max = 100;
var a[100];

procedure sum(var n);
var i, ret, b[10];
    procedure min(n);
    var i, mn;
        procedure getmin(var x; var y);
        begin
            if x > y then x := y;
            while odd y do
            begin
                y := y - 1;
            end;
        end;
    begin
        for i := 1 to n do
        begin
            call getmin(a[i], a[n]);
        end;
    end;
begin
    ret := 0;
    for i := 1 to a[i] do
    begin
        ret := ret + a[i] * ret / b[1];
        call min(i);
    end;
end;

begin
    call sum(a[1]);
    a[max] := a[1];
end.
